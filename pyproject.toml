[project]
name = "eclipsebord"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.5",
]

[tool.uv.workspace]
members = [
    "backend",
    "frontend",
] 

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
